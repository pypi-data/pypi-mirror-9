{% load djinn_i18n_tags %}

<div class="entry well">
  <header>
    <h3>{{ entry.msgid }}
      {% if entry|is_override:view.locale %}
      <span class="label label-warning">override</span>
      {% endif %}
    </h3>
  </header>

  {% if entry.msgctxt %}
  <p class="text-info">{{ entry.msgctxt }}</p>
  {% endif %}

  {% if entry.comment or entry.tcomment %}
  <p class="text-info">
    {{ entry.comment}} 
    {{ entry.tcomment}}     
  </p>
  {% endif %}
  
  <h4>Translation
    <a href="{% url 'djinn_i18n_trans' entry.msgid view.locale %}"
       class="btn btn-xs btn-success">
      <i class="fa fa-flag"></i> Translate
    </a>
  </h4>
  <p>{{  entry.msgstr }}</p>
</div>
{% endfor %}

{% endblock %}
