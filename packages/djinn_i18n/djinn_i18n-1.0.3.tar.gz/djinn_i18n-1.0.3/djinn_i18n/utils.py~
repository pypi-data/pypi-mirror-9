from django.conf import settings


def get_override_base():

    return settings.PO_OVERRIDES or "%s/var/locale" % settings.PROJECT_ROOT


def generate_po_path(base, locale):

    return "%s/%s/LC_MESSAGES/django.po" % (base, locale)
