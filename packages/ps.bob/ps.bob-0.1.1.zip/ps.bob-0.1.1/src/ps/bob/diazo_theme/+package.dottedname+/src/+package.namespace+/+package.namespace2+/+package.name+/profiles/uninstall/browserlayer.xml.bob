<?xml version="1.0"?>
<layers>
  <layer
      name="{{{ package.dottedname }}}"
      interface="{{{ package.dottedname }}}.interfaces.{{{ package.browserlayer }}}"
      remove="True"
      />
</layers>
