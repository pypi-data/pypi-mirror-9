{% extends "djinn_contenttypes/snippets/modal.html" %}

{% load bootstrap i18n %}

{% block id %}contentimages{% endblock %}
  
{% block title %}{% trans "Add image" %}{% endblock %}

{% block body %}

<div id="contentimages">

  {% if not view.has_images %}
  {% trans "No images found" %}
  {% endif %}

  {% for item in view.list_images %}
  <div class="clearfix">

    <img class="pull-left" src="{{ item.get_thumbnail_url }}" border="0"/>

    <ul class="controls pull-left unstyled">
      <li>
        <a href="{{ item.get_display_url }}"         
           data-img="{{ item.get_wysiwyg_include_lr_url }}"
           data-pos="left"
           >
          {% trans "left" %}
        </a>
      </li>
      <li>
        <a href="{{ item.get_display_url }}"         
           data-img="{{ item.get_wysiwyg_include_c_url }}"
           data-pos="center"
           >
          {% trans "center" %}
        </a>
      </li>
      <li>
        <a href="{{ item.get_display_url }}"         
           data-img="{{ item.get_wysiwyg_include_lr_url }}"
           data-pos="right"
           >
          {% trans "right" %}
        </a>
      </li>
    </ul>
  </div>
  {% endfor %}
  
</div>
{% endblock %}

{% block footer %}
<a href="#" class="btn cancel">{% trans 'Cancel' %}</a>
{% endblock %}
