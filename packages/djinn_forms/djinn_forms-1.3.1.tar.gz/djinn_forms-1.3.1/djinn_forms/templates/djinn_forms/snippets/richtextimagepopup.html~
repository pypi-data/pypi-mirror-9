{% extends 'layout-modal.html' %}

{% block header %}Voeg afbeelding toe{% endblock %}

{% block body %}

    <div class="image-selector">

      {% if images|length_is:"0" %}
        Geen afbeeldingen gevonden.
      {% endif %}

        {% for item in images %}
            <div style="float:left;margin-bottom: 5px;" class="cb">
                <img src="{{ item.get_product_media_thumb_url }}" border="0"/>
            </div>
            <div class="imgactions" style="float:left;margin-left:20px;padding-top:20px;">
                <a href="#"
                   onclick="parent.pg.insert_image_wysiwyg('left', '{{ item.get_display_url }}','{{ item.get_wysiwyg_include_lr_url }}');return false;">Links
                    v/d pagina</a>
                <br/>
                <a href="#"
                   onclick="parent.pg.insert_image_wysiwyg('center', '{{ item.get_display_url }}','{{ item.get_wysiwyg_include_c_url }}');return false;">Midden
                    v/d pagina</a>
                <br/>
                <a href="#"
                   onclick="parent.pg.insert_image_wysiwyg('right', '{{ item.get_display_url }}','{{ item.get_wysiwyg_include_lr_url }}');return false;">Rechts
                    v/d pagina</a>
            </div>
            <div class="cb"></div>
        {% endfor %}

    </div>

{% endblock %}

{% block footer %}
    <a href="#" class="btn cancel">Annuleren</a>
{% endblock %}
