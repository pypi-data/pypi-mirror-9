from distutils.core import setup

setup(
    name            = 'nestprint',
    version         = '1.6.0',
    py_modules      = ['nestprint'],
    author          = 'bfunkt',
    description     = 'prints each element of nested lists',
    )
    
