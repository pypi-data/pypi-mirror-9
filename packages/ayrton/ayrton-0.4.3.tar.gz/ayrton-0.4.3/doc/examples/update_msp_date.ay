#! /usr/bin/env ayrton -f

# source (bash ('~tsr_tds/soft_env')[0])

with cd ("%s/tds_web" % HOME):
    with ssh ('xxxx@foo.megacorp.net') as s:
        with cd (bash ('/farequote/MSPUpdate/DSR/Keys/FQ-Detector-*-%s' % arg[1])[0]):
            file= bash ('*_LOG')[0]
            print ("File: %s" % file)
            cat (file)

    python ('scripts/parse_msp_emails.py', _in=s[1])
