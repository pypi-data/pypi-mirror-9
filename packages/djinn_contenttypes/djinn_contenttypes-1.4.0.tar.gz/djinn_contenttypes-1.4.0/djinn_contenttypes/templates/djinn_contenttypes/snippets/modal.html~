{% load bootstrap i18n %}

<div class="modal hide fade" id="sharecontent">

  <div class="modal-header">
    <a class="close" data-dismiss="modal">x Sluit venster</a>
    <h3></h3>
  </div>

  <div class="modal-body">
    <form method="POST"
          action="{% url 'djinn_contenttypes_share' view.obj.ct_name view.obj.id %}"
          id="retweet_form"
          class="modal-submit-inline">

	  {% if form.errors %}
    <div class="alert alert-error">
      {% for field in form %}
      {% if field.errors %}
      <dl>
        <dt>{{ field.label }}</dt><dd>{{ field.errors }}</dd>
      </dl>
      {% endif %}
      {% endfor %}
    </div>
    {% endif %}

    <div class="general-help alert alert-info">
      {{ form.help|safe }}
    </div>

    <div class="control-group required">
      <label class="control-label">{{ view.page_title }}</label>
      <div class="controls">

        <label class="radio">
          {{ view.render_recipients.0|removetags:'label' }}
        </label>

        <label class="radio">
          {{ view.render_recipients.1|removetags:'label' }}
        </label>

        <div class="controls hide collapsed">
          {{ form.group }}
        </div>

        <label class="radio">
          {{ view.render_recipients.2|removetags:'label' }}
        </label>

        <div class="controls hide collapsed">
          {{ form.user }}
        </div>

      </div>
    </div>

    {{ form.message|bootstrap }}

    <div class="btn-group form-actions">
      <a href="#" class="btn btn-primary submit">{% trans 'Share' %}</a>
      <a href="#" class="btn cancel">{% trans 'Cancel' %}</a>
    </div>

  </form>
  
</div>

<script>
  $("#sharecontent .relate").each(function() {
    djinn.forms.initRelateWidget($(this));
  });
</script>
