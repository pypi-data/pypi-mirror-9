{% load i18n %}

{% if obj.is_deleted %}
<del title="{{ obj.ct_label }} is verwijderd">{{ obj.title }}</del>
            obj.ct_label, obj.title)
    elif not obj.is_published:
        title = """<a href="%s">%s <span></span></a>""" % (
            obj.url, obj.title)
    else:
        title = """<a href="%s">%s</a>""" % (obj.url, obj.title)

    return title

