You'll notice there is a settings_local.py and settings.py, which to use?

Trick question, you are actually using both because settings.py imports settings_local to add or override any values necessary.

settings.py is what all instances use,
settings_local is ... local
