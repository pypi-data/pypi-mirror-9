Usage:
  config print
  config reload
  config help

'config' is used with the following subcommands:
  print     Prints the current configuration context (both local and global config options).
  reload    Reloads the current config context (both local and global files).
  help	    Display this screen.
