angular.module(
    'opal.controllers'
).controller(
    'FlowCtrl',
    function($scope){

        // The patient is 'entering'. Do the right thing.
        $scope.enter = function(){
            
        };

        // The patient is 'leaving'. Do the right thing.
        $scope.exit = function(){
            
        };

        $scope.$on('flow', function(event, verb, schema, options, config){
            
        })
    }
);
