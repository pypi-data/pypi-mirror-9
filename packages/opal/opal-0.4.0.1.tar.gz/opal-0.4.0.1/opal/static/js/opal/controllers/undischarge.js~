angular.module('opal.controllers').controller(
    'ConfirmUndischargeCtrl',
    function($scope, $modalInstance, episode){

        $scope.confirm = function(){
            $modalInstance.close(true);
        }

        $scope.cancel = function(){
            $modalInstance.close(null);
        }
        
    }
);
