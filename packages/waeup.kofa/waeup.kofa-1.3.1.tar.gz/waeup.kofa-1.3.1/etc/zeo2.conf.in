<runner>
  program ${buildout:directory}/bin/paster serve --pid-file ${buildout:directory}/var/zeo2.pid ${buildout:directory}/parts/etc/themed-zeo2.ini
  daemon on
  transcript ${zope_conf:logfiles}/zdaemon-zeo2.log
  socket-name ${zope_conf:logfiles}/zeo2.sock
  # Enable this to run the child process as a different user
  # user zope
</runner>
