<runner>
  program ${buildout:directory}/bin/paster serve --pid-file ${buildout:directory}/var/zeo1.pid ${buildout:directory}/parts/etc/themed-zeo1.ini
  daemon on
  transcript ${zope_conf:logfiles}/zdaemon-zeo1.log
  socket-name ${zope_conf:logfiles}/zeo1.sock
  # Enable this to run the child process as a different user
  # user zope
</runner>
